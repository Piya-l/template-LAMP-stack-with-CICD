# Production Apache Configuration
# Add your production-specific Apache directives here.
# For example, stricter logging, SSL configuration, performance optimizations.

# Example: Disable directory listings for security
<Directory /var/www/html>
    Options -Indexes
    AllowOverride All
    Require all granted
</Directory>

# Example: Set production logging paths (ensure these directories exist in your image)
# ErrorLog /var/log/apache2/error.log
# CustomLog /var/log/apache2/access.log combined
